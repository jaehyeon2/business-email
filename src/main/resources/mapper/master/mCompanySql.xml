<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jaehyeon2.be.dao.master.MCompanyDao">

	<insert id="istCompany" parameterType="map">
		INSERT INTO company (
			company_name,
			company_info,
			company_meet,
			company_email_content,
			company_status,
			company_delete,
			insert_time,
			update_time
		) VALUES (
			#{companyName},
			#{companyInfo},
			#{companyMeet},
			#{companyEmailContent},
			#{companyStatus},
			'0',
			NOW(),
			NOW()
		)
	</insert>
	
	<update id="udtCompany" parameterType="map">
		UPDATE 
			company
		SET
			<if test="companyName != null and companyName != ''">
			company_name = #{companyName},
			</if>
			<if test="companyInfo != null and companyInfo != ''">
			company_info = #{companyInfo},
			</if>
			<if test="companyMeet != null and companyMeet != ''">
			company_meet = #{companyMeet},
			</if>
			<if test="companyEmailContent != null and companyEmailContent != ''">
			company_email_content = #{companyEmailContent},
			</if>
			<if test="companyStatus != null and companyStatus != ''">
			company_status = #{companyStatus},
			</if>
			update_time = NOW()
		WHERE 
			company_no = #{companyNo}
			AND company_delete = '0'
	</update>
	
	<update id="dltCompany" parameterType="map">
		UPDATE 
			company
		SET
			company_delete = '1',
			update_time = NOW()
		WHERE 
			company_no = #{companyNo}
			AND company_delete = '0'
	</update>

	
</mapper>